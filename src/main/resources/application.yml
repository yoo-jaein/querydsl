spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver

    jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
#          show_sql: true
          format_sql: true
          user_sql_comment: true #jpql 주석으로 보기

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace